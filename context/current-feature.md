# Current Feature: Admin Item CRUD — Phase 2 (Delete)

## Status

In Progress

## Goals

- Wire the existing scaffolded Delete button in `AdminItemActions` to a real `deleteItemAction` server action that removes an `Item` row from Prisma after confirmation.
- Use shadcn `AlertDialog` for the confirmation step (Title: "Delete Device", Description: "This action cannot be undone.", Cancel + Confirm Delete actions).
- Enforce the critical guard server-side: an item with status `IN_USE` cannot be deleted. The DAL returns a typed `IN_USE` error so the UI can show a clean Sonner toast instead of a Prisma stack trace.
- Disable the Delete trigger in the UI when `item.status === 'IN_USE'` with a tooltip ("Items in use cannot be deleted") for defense-in-depth — the server guard remains the source of truth.
- Use destructive styling for the Confirm Delete action so the destructive intent is visually clear.
- Revalidate `/admin` and `/hardware` after a successful delete so both inventory views drop the row.
- Sonner success/failure toasts mirror the Phase 1 admin-item create / edit / repair-toggle pattern.

## Notes

- Spec: [admin-item-crud-2-spec.md](context/features/admin-item-crud-2-spec.md). Out of scope: soft delete / archival, audit logs, rental-history cleanup, bulk delete.
- DAL in `src/lib/db/item-mutations.ts` (existing) — extend with a `deleteItem({ itemId })` function that wraps a `prisma.$transaction` and uses `deleteMany({ where: { id, status: { not: 'IN_USE' } } })` so a concurrent rent right between the read and the delete cannot succeed past the IN_USE guard. If `count === 0`, fall back to a follow-up `findUnique` to distinguish `NOT_FOUND` vs `IN_USE` for the error code.
- Reuse the existing `ItemMutationErrorCode` union (`NOT_FOUND` | `IN_USE` | `INVALID_STATUS` | `RACE`) and `ITEM_MUTATION_ERROR_MESSAGE` map — they already have `NOT_FOUND` and `IN_USE` copy that fits this flow.
- Server action in `src/actions/items.ts` (existing) — add `deleteItemAction({ itemId })`: `requireAdmin()` → zod-validate `itemId` → call `deleteItem` → on success `revalidatePath('/admin')` + `revalidatePath('/hardware')` → return `{ success, error? }`. Same shape as the other admin-item actions.
- Cascade: the Prisma schema already has `onDelete: Cascade` on `RentalHistory.itemId`, so deleting an `Item` row automatically removes its history. No manual cleanup needed and the spec explicitly excludes rental-history cleanup from scope.
- AlertDialog primitive: shadcn `alert-dialog` is not yet installed in `src/components/ui/`. Add it via `npx shadcn@latest add alert-dialog` and use it directly — keep the dialog colocated in a new `DeleteDeviceDialog` client component (similar shape to the existing `EditDeviceDialog`), not inlined into `AdminItemActions`.
- Loading / disabled state: drive the Confirm Delete button via `useTransition`; show "Deleting..." copy while pending and disable Cancel + the trigger to prevent double-submits. Close the dialog on success before the toast fires; leave it open on failure so the user sees the error and can retry or cancel.
- `AdminItemActions` already shows a disabled Trash2 button — replace it with the new `DeleteDeviceDialog` trigger. Keep the button visually destructive (`variant="destructive"` if the shadcn Button primitive supports it, else custom red styling matching the existing repair button's tone).
- Migration discipline: no schema changes needed (the `Item` model already supports deletion via Prisma). No migration this phase.
- Existing Playwright e2e suite (auth / rental / admin-user / no AI test) still passes. The rental test rents and returns the same item, so it never deletes anything; the admin-user test creates users but doesn't touch items. No regression risk if we keep button labels / aria-labels consistent.
- Vitest still not installed — the `deleteItem` DAL `IN_USE` guard and the new error-code path through `deleteItemAction` are good unit-test targets queued for the Vitest-scaffold follow-up.

## History

<!-- Keep this updated. Earliest to latest -->

- 2026-04-27 — Initial setup of Next.js 16 (App Router) with React 19, TypeScript, Tailwind CSS v4, ESLint v9 (flat config), and Vitest test scripts scaffolded.
- 2026-04-27 — Dashboard UI Phase 1: initialized shadcn/ui (components.json, theme tokens in globals.css, lib/utils, button primitive), defaulted the app to dark mode, and added the `/dashboard` route with sidebar + main layout placeholders.
- 2026-04-27 — Dashboard UI Phase 2: added shadcn sidebar/sheet/avatar/tooltip primitives, built `AppSidebar` (Hardware Hub header logo, three nav links with collapsed-icon tooltips, user footer from mock-data + logout placeholder), wired `SidebarProvider`/`TooltipProvider` into the dashboard layout with a mobile-only `SidebarTrigger` (drawer below 768px), and refactored `useIsMobile` to `useSyncExternalStore`.
- 2026-04-27 — Dashboard UI Phase 3: populated `/dashboard` with the hardware list view — `ItemCard` (name + color-coded status badge, brand, formatted purchase date, optional assignee + notes) and `HardwareList` client component owning case-insensitive substring search across name/brand/notes plus Name/Brand/Date/Status sort buttons, rendered in a responsive 1/2/3-col grid from mock items.
- 2026-04-27 — Refined UI: swapped Geist Mono → JetBrains Mono and renamed font CSS variables to `--font-sans`/`--font-mono` (headings now mono); added a `--brand` cyan-400 token and applied `text-brand`/`bg-brand` to the logo, search icon, avatar fallback, active sort button, assigned-to email, and Rent button; replaced `SidebarRail` with an explicit `SidebarTrigger` in the sidebar header; ItemCard gained a per-status right-edge stripe (`border-r-4`), subtle hover lift + shadow, and a decorative Rent button on AVAILABLE items only.
- 2026-04-27 — Item Cards: added shadcn `button-group` primitive; flipped the per-status card stripe from the right edge to the left edge; regrouped grid-card content into header / meta / body / action sections with `mt-auto` Rent for cross-card alignment; introduced a `view` prop on `ItemCard` plus a list-row layout (inline name · brand · date · assigned with truncated notes underneath); converted the sort controls into a segmented `ButtonGroup` and added a `LayoutGrid` / `List` view-mode toggle on the right of the toolbar with active state in the cyan brand color.
- 2026-04-28 — Prisma + Neon Postgres: installed Prisma 7 with the new `prisma-client` generator (output to `src/generated/prisma`, gitignored) + `@prisma/adapter-neon` over `@neondatabase/serverless`; added `prisma.config.ts` reading `DATABASE_URL` from `.env`; authored the initial schema (User, Item, RentalHistory + Role/ItemStatus/RentalAction enums, NextAuth Account/Session/VerificationToken, indexes, cascade deletes); created `src/lib/prisma.ts` with a globalThis singleton; bumped `tsconfig` `target` to ES2023; generated and applied the `init` migration to the Neon dev branch.
- 2026-04-28 — Database Seed: wired `migrations.seed` in `prisma.config.ts` to `tsx prisma/seed.ts`; added `bcryptjs` (12-round hashing); authored an idempotent `prisma/seed.ts` that upserts admin (`admin@booksy.com`), `j.doe@booksy.com`, and `a.smith@booksy.com` (added so item_10 and existing rental history FKs resolve), then upserts all 11 mock items and 5 rental history entries from `src/lib/mock-data.ts` using their stable string IDs.
- 2026-04-28 — Dashboard Items from DB: added `src/lib/db/items.ts` with `getItems()` that pulls items from Prisma (`orderBy: name asc`) and serializes `purchaseDate`/`returnDate` to ISO date strings to keep the existing `Item` type contract; converted `/dashboard/page.tsx` to an async server component awaiting `getItems()` with `export const dynamic = "force-dynamic"` so each request re-queries Neon; left `mock-data.ts`, `HardwareList`, and `ItemCard` untouched.
- 2026-04-28 — My Rentals UI + Route Restructure: introduced `(app)` route group so the sidebar layout covers multiple pages; renamed the hardware list route from `/dashboard` to `/hardware` and updated the sidebar logo link accordingly; added `/my-rentals` async server component (force-dynamic) backed by `src/lib/db/rentals.ts` `getMyRentals(email)` which filters items by `assignedTo + IN_USE` and orders by `returnDate`; added `src/lib/auth.ts` `getCurrentUserEmail()` returning `j.doe@booksy.com` as a placeholder until NextAuth lands; copied the dashboard `ItemCard` into `src/components/my-rentals/item-card.tsx` with a Return button (always shown) plus an optional `due` prop rendering a Clock + formatted deadline; built `MyRentalsList` with the same grid/list view-mode toggle as `HardwareList` (no search/sort) and a basic empty state.
- 2026-04-28 — Prod DB Seed + README: installed `dotenv-cli`; added `db:migrate:deploy:prod`, `db:seed:prod`, `db:test:prod` npm scripts that pipe `.env.production` through dotenv-cli so the prod target is always explicit; applied the existing migration and ran the existing seed against the prod Neon branch (3 users, 11 items, 5 rental history entries — verified via `db:test:prod`); replaced the create-next-app default README with a project README led by a Demo Accounts table (admin/j.doe/a.smith with seeded passwords), followed by quick-start, scripts reference, tech stack, and TODO sections for the fuller README content (status, trade-offs, AI dev log, screenshots).
- 2026-04-28 — Admin Panel Phase 1 + Card Extraction: extracted the dashboard `ItemCard` to a shared `src/components/items/item-card.tsx` with an `action?: ReactNode` slot, optional `due`, and a `showAssignee` toggle; migrated `HardwareList` (passes `<RentButton />` only when AVAILABLE) and `MyRentalsList` (passes `<ReturnButton />` always + `due` + hides assignee) to the shared card and deleted both duplicate copies; added shared `RentButton`/`ReturnButton` in `src/components/items/actions.tsx`. Installed shadcn `dialog`/`select`/`textarea`/`label` primitives plus `react-hook-form`, `zod`, `@hookform/resolvers` (the shadcn `form` component isn't shipped with the `base-nova` style, so the Add Device dialog wires RHF + zod directly). New `/admin` route inside the `(app)` group: server component wraps `<Suspense fallback={<InventorySkeleton />}>` around an `AdminInventoryLoader` that calls `getItems()`, derives distinct brand options, and renders `AdminInventory` (search + Name/Brand/Date/Status sort + grid/list view toggle, mirroring `HardwareList`). Each card shows a disabled `AdminItemActions` cluster (Edit / Wrench / Delete) and a cyan "Add Device" button in the header opens `AddDeviceDialog` whose zod-validated submit currently `console.log`s the payload and resets the form.
- 2026-04-28 — Admin Panel Phase 2 + ViewToggle Extraction: extracted the duplicated grid/list `ButtonGroup` into a shared `src/components/items/view-toggle.tsx` and migrated `HardwareList`, `MyRentalsList`, and `AdminInventory` to use it. Added `src/lib/db/users.ts` `getUsers()` returning a UI-safe `UserListItem` shape (id, name, email, role, createdAt — no password). Built `UserCard` with grid + Google-Drive-style list variants (avatar/initials, name, email, role badge, joined date, action slot), `AdminUserActions` disabled Edit/Delete cluster, `UsersSkeleton`, and `CreateUserDialog` (shadcn Dialog + RHF + zod for Name/Email/Password/Role; submit logs payload + resets). New `UsersList` client component renders the toolbar (count + view toggle + Create User cyan button) and the grid/list. `/admin/page.tsx` now sequentially renders two Suspense-bounded sections: inventory then users.
- 2026-04-29 — Auth Phase 1 (NextAuth v5 Credentials Foundation): installed `next-auth@beta` + `@auth/prisma-adapter`; added split config — edge-safe `src/auth.config.ts` (with `pages.signIn: '/login'` and an `authorized` callback that gates `/hardware`, `/my-rentals`, `/admin` and redirects authed users at `/` or `/login` to `/hardware`) and full `src/auth.ts` attaching `PrismaAdapter`, JWT session strategy, a Credentials `authorize` that validates via `prisma.user.findUnique` + `bcrypt.compare`, and `jwt`/`session` callbacks copying `id`/`role` onto the session. Added `src/app/api/auth/[...nextauth]/route.ts`, `src/proxy.ts` (Next.js 16's renamed middleware) wrapping `auth` with a matcher that skips `/api/auth`, Next internals, and static assets, and `src/types/next-auth.d.ts` augmenting `Session.user` and `User` with `id`/`role`. Replaced the placeholder `getCurrentUserEmail()` to read from the live session (throws if no session). Added a minimal client-side `/login` page (email/password → `signIn('credentials')` → `/hardware`) as the redirect target for `pages.signIn`; full Phase 2 styling lands later. Converted `(app)/layout.tsx` to an async server component that awaits `auth()`, redirects to `/login` if no session, and passes session-derived user info to `AppSidebar`; the sidebar now renders the real name/role and the logout button calls `signOut({ redirectTo: '/login' })`. Documented `AUTH_SECRET`/`AUTH_URL` in `.env.example`.
- 2026-04-29 — Auth Phase 2 (Login UI + Sidebar UserNav): added shadcn `card` primitive; rewrote `src/app/login/page.tsx` as a server component rendering a centered `Card` with a Wrench + "Hardware Hub" mono title, "Sign in to manage equipment" description, and the new `<LoginForm />` body. Built `src/components/auth/login-form.tsx` — RHF + zod (`z.email()` + `password.min(1)`), inline per-field error messages, a single muted "Invalid credentials" alert when `signIn('credentials', { redirect: false })` returns `{ error }`, an `isSubmitting`-driven disabled state with "Signing in..." copy, and a cyan brand-token Sign In button; success calls `router.push('/hardware')` + `router.refresh()` so the layout re-reads the session. Extracted the sidebar user footer into `src/components/auth/user-nav.tsx` (`UserNavUser` type re-exported, avatar + initials fallback derived from name-or-email split on whitespace/`@`/`.`, name → email muted line → uppercase tracked role line, `signOut({ redirectTo: '/login' })` icon button, all hidden under `group-data-[collapsible=icon]`). Slimmed `AppSidebar` down to nav + `<SidebarFooter><UserNav user={user} /></SidebarFooter>`; the `(app)/layout.tsx` already feeds it the session-derived user shape from Phase 1.
- 2026-04-29 — Auth Phase 3 (Roles & Protected Data): moved the `jwt` + `session` callbacks from `src/auth.ts` into the edge-safe `src/auth.config.ts` so the proxy's `authorized` callback can read `auth.user.role` (Phase 1 had them in `auth.ts`, which left `role` undefined on the edge — admins were getting bounced from `/admin` because `auth?.user?.role !== 'ADMIN'` was always true). Extended `authorized` with an admin-route gate: authed-but-non-admin hitting `/admin` or `/admin/*` → `NextResponse.redirect('/hardware')`; unauth users still bounce to `/login`. Added `getSession()` / `requireSession()` (redirect to `/login`) / `requireAdmin()` (redirect to `/hardware`) helpers in `src/lib/auth.ts`, rewired `getCurrentUserEmail()` on top of `requireSession()`. Called `await requireAdmin()` at the top of `src/app/(app)/admin/page.tsx` for defense-in-depth. Filtered the Admin Panel entry out of `NAV_ITEMS` in `AppSidebar` when `user.role !== 'ADMIN'`. `/my-rentals` was already session-scoped from Phase 1 so no change there. Existing JWT cookies issued before this change don't carry `role` — users have to re-login once to pick it up.
- 2026-04-29 — Rental Workflow Phase 1 (Rent / Return Server Actions): added centralized DAL in `src/lib/db/rental-actions.ts` — `rentItem()` and `returnItem()` each wrap their guards + `Item` mutation + `RentalHistory` insert in `prisma.$transaction`, returning a typed `RentalResult` (`NOT_FOUND`, `IN_REPAIR`, `ALREADY_RENTED`, `NOT_IN_USE`, `NOT_ASSIGNED_TO_USER`, `RACE`); race protection via `updateMany` filters re-checking `status` (and `assignedTo` for return) so a `count === 0` aborts before any history row is written. Default rental period is `+14d` from `DEFAULT_RENTAL_DAYS`. Added thin `'use server'` actions in `src/actions/rentals.ts` (`rentItemAction` / `returnItemAction`) that `requireSession()`, zod-validate the `itemId`, call the DAL with the session-derived `userId`/`userEmail` (never client-supplied), `revalidatePath('/hardware')` + `revalidatePath('/my-rentals')` on success, and return `{ success, error? }` with messages from `RENTAL_ERROR_MESSAGE`. Converted `RentButton` / `ReturnButton` in `src/components/items/actions.tsx` to client components that take `itemId`, drive a disabled "Renting..." / "Returning..." state via `useTransition`, and surface action errors as a small inline `role="alert"` line under the button (success silently disappears via revalidation). Wired `item.id` through `HardwareList` and `MyRentalsList`. Vitest still isn't actually installed in `package.json` despite the `npm test` command living in `CLAUDE.md` — tests for the rental DAL guards/state-transitions are deferred to a follow-up that scaffolds Vitest.
- 2026-04-29 — Rental Workflow Phase 2 (Confirmation Modals + Sonner + Optimistic UI): extracted pure helpers into `src/lib/rental-status.ts` (`RENTAL_PERIOD_OPTIONS = [7, 14, 30]`, `DEFAULT_RENTAL_PERIOD_DAYS = 14`, `addDays`, and `classifyDue` — overdue when `returnDate < startOfTodayUtc`, due-soon when within 3 days). Extended the Phase 1 DAL `rentItem()` to accept optional `rentalDays` (still defaults to 14) and `rentItemAction` to zod-validate it as a `7 | 14 | 30` literal union so the client can't ship arbitrary periods. Installed shadcn `sonner` and patched the generated component to drop the `useTheme`/`next-themes` import (the project sets `<html class="dark">` directly — it doesn't use `next-themes`) and hardcode `theme="dark"`; mounted `<Toaster position="top-right" richColors closeButton />` once in the root `app/layout.tsx`. New `RentDialog` and `ReturnDialog` in `src/components/items/` — Rent shows item summary + a 7/14/30 `ButtonGroup` deadline preset with a Calendar "Returns by …" preview, Return shows item summary + a confirm prompt; both close the dialog urgently on confirm, then open a `useTransition` to call the action and surface a Sonner success/error toast (errors reuse `RENTAL_ERROR_MESSAGE` copy via `res.error`). Replaced the inline `RentButton` / `ReturnButton` callers — `HardwareList` and `MyRentalsList` now render the dialogs directly and the old `actions.tsx` was deleted. Both lists wrap their items prop in `useOptimistic` so confirming a rent flips the item's status to `IN_USE` immediately (and the Rent dialog disappears) and confirming a return filters the item out of `/my-rentals` immediately; revalidation is still the source of truth, so a `{ success: false }` result resets the optimistic state when the unchanged source props re-flow. Toast lives at module scope (Sonner singleton), so it still fires even when the dialog component unmounts mid-transition due to the optimistic state change. `ItemCard` `DueLine` gained a `state` prop driving normal / yellow due-soon / red `animate-pulse` overdue treatments (and swaps the Clock icon for `AlertTriangle` on overdue); `MyRentalsList` passes `dueState={classifyDue(item.returnDate)}` per card. Vitest is still not installed — `classifyDue`, the rentalDays zod enum, and the DAL `rentalDays` plumbing are good unit-test targets queued for the Vitest-scaffold follow-up.
- 2026-04-29 — Admin Item Management MVP (Create / Edit / Repair Toggle): added centralized DAL in `src/lib/db/item-mutations.ts` — `createItem`, `updateItem`, and `toggleRepair`, with a typed `ItemMutationErrorCode` union (`NOT_FOUND`, `IN_USE`, `INVALID_STATUS`, `RACE`) and `ITEM_MUTATION_ERROR_MESSAGE` map. `toggleRepair` runs in `prisma.$transaction` and uses `updateMany` with a status filter so a concurrent rent can't race past the IN_USE guard; `updateItem` rejects `IN_USE` if the caller tries to flip status on a currently-rented item. Three `'use server'` actions in `src/actions/items.ts` (`createItemAction` / `updateItemAction` / `toggleRepairAction`) that all `requireAdmin()` first, zod-validate the payload (status enum locked to `AVAILABLE | REPAIR` — `IN_USE` is owned by the rental workflow), call the DAL, and `revalidatePath('/admin')` + `revalidatePath('/hardware')` on success. `purchaseDate` strings are normalized to `Date | null` at the action boundary. Extracted the existing `AddDeviceDialog` body into a shared `src/components/admin/device-form.tsx` (RHF + zod, Controller-driven `Select`s, mode-aware submit copy, and a `lockStatus` prop that renders the status field as a read-only "In Use" input with helper text). `AddDeviceDialog` now wires the form to `createItemAction` with Sonner success/error toasts; new `EditDeviceDialog` (Pencil trigger) prefills from the item, locks status when `item.status === 'IN_USE'`, and calls `updateItemAction`. Both dialogs reset on close by bumping a `formKey` so the React form remount discards in-flight values. `AdminItemActions` now hosts the `EditDeviceDialog` trigger and a Wrench button that fires `toggleRepairAction` via `useTransition` (disabled with a tooltip when status is IN_USE); Delete stays disabled (out of scope this phase). `AdminInventory` threads `item` + `brandOptions` into each card's `AdminItemActions`. Vitest is still not installed — `toggleRepair`'s IN_USE guard, the create-status zod rejection of `IN_USE`, and the update-status lock are good unit-test targets queued for the Vitest-scaffold follow-up.
- 2026-04-30 — Admin User Management MVP (Create User Persistence): added DAL in `src/lib/db/user-mutations.ts` — `createUser` hashes the password via `bcrypt.hash(plain, 12)` (matching `prisma/seed.ts` and `src/auth.ts`'s Credentials authorize so hashes are interchangeable), inserts via Prisma, and catches `Prisma.PrismaClientKnownRequestError` with code `P2002` to return a typed `{ ok: false, error: 'DUPLICATE_EMAIL' }`; exports `USER_MUTATION_ERROR_MESSAGE` for UI copy. Added `'use server'` `createUserAction` in `src/actions/users.ts` — `requireAdmin()` first, then zod-validates `name` / `email` (via `z.email()` with `.trim().toLowerCase()` so case-only differences can't bypass the unique-email guard) / `password.min(8)` / `role` enum, calls the DAL, and `revalidatePath('/admin')` on success. Rewired the existing scaffolded `CreateUserDialog` to await `createUserAction` instead of `console.log` — Sonner success toast (`name` + `email` description) closes the dialog; failure toast leaves the dialog open. Bumped the form's password schema from `min(1)` to `min(8)`, swapped the submit button copy to "Creating..." while pending, and added a `formKey` bump on close so the form remounts with cleared state (matching the device dialogs pattern). Out of scope: edit / delete / deactivate / password reset (deferred). Vitest still not installed — the `createUser` duplicate-email branch and the bcrypt-12-round invariant are good unit-test targets queued for the Vitest-scaffold follow-up.
- 2026-04-30 — Critical E2E Tests (Playwright): installed `@playwright/test` + Chromium, added `playwright.config.ts` (Chromium-only project, `baseURL: http://localhost:3000`, `webServer` running `npm run dev` with `reuseExistingServer`, `workers: 1` for serial DB-touching tests), and wired `npm run test:e2e` / `test:e2e:ui` scripts. Gitignored `/test-results`, `/playwright-report`, `/playwright/.cache`. Shared `tests/e2e/helpers/auth.ts` exports `ADMIN` / `REGULAR_USER` credentials matching the seed and `login` / `logout` helpers. Three independent specs: `auth.spec.ts` (Test 1: unauthed `/hardware` → `/login`; user login → `/hardware`; user blocked from `/admin` → `/hardware`; admin login + `/admin` lands on the admin panel), `rental.spec.ts` (Test 2: pick the first AVAILABLE card, capture its `<h3>` text, confirm rent, assert toast + Rent button gone, find the same item on `/my-rentals`, confirm return, assert toast + card gone), and `admin-user.spec.ts` (Test 3: admin opens Create User, fills `Test User` with email `test.user.e2e+${Date.now()}@booksy.com` so re-runs don't trip the unique-email guard, submits, asserts modal closes + toast + new user appears in the list, logs out, then logs in as the new user landing on `/hardware`). Sonner gotcha: toasts don't expose `role="status"` despite the prior phase notes — they carry `data-sonner-toast` on the `<li>`, so the assertions use `page.locator('[data-sonner-toast]').filter({ hasText })`. Suite passes locally: `3 passed (10.6s)`. This phase replaces the Vitest carryover from prior phases — testing strategy is now end-to-end against the seeded Neon dev DB; mock-driven unit tests can come later if/when value justifies it.
- 2026-04-30 — AI Semantic Search MVP (gpt-5-nano): installed the official `openai` SDK and added a server-only core in `src/lib/ai/search.ts` — `aiSearchItemIds(query, items)` caches a single `OpenAI` client, returns `[]` (with a console warning) when `OPENAI_API_KEY` is missing, caps the query at 200 chars and the catalog at 50 items server-side, calls `gpt-5-nano` via `chat.completions.create` with `response_format: { type: 'json_object' }`, parses the response defensively, and validates returned ids against the input catalog so the model can't invent ids; any throw is caught and returns `[]`. Added `'use server'` `aiSearchItemsAction` in `src/actions/search.ts` — `requireSession()` first, zod-validates `query.min(1).max(200)` and the items array (`{ id, name, brand, status, notes }` only — `assignedTo` / `returnDate` / `purchaseDate` deliberately stripped to keep the prompt small and avoid leaking PII to OpenAI), then calls the lib. `HardwareList` gained a `mode: 'basic' | 'ai'` `ButtonGroup` toggle (Search / Sparkles icons), `aiResultIds: string[] | null` state (null = AI search not yet run), and a `useTransition` driving the loading state. Basic mode keeps the existing real-time fuzzy filter; AI mode disables real-time filtering and fires the action only on Enter (so we don't burn tokens), swaps the leading icon for a spinning `Loader2`, and disables the input while pending. Empty-state copy branches: AI mode + ran + zero results → "No AI matches found"; basic mode + has query + zero results → "No items match your search."; default → "No items to show.". Toggling modes resets `aiResultIds` so a stale prior run doesn't leak. Existing Playwright suite (auth / rental / admin-user) still passes — the AI flow is gated by Enter-on-AI-mode, so the basic-search-driven rental test path is untouched. Caveat surfaced during smoke-test: a working OpenAI account *with* a payment method and credits is required — a fresh free-tier key returns `429 insufficient_quota`, which we catch + log as `[ai-search] OpenAI call failed` and silently fall back to `[]`. Document this in the README's setup section if/when AI search is part of the demo.
